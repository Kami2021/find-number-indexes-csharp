# find-number-indexes-csharp
A simple C# console application that finds all indexes of a target number in a list of integers. The program takes user input, validates integers, and returns all positions where the target value appears. Useful for beginners learning arrays, loops, and input validation in C#.
